/* MouldMaster process-data deep dive — 50 additional synthetic evidence cases */
(function(){
'use strict';
const VERSION='2026.08.26.1',REVIEWED='2026-08-26',REVIEW_BY='2026-11-26';
const BASE=window.MM_PROCESS_DATA_DIAGNOSTICS,E=window.MM_EVIDENCE_SOURCES,PACKS=window.MM_PROCESS_DATA_DEEP_DIVE_PACKS||[];
if(!BASE)throw new Error('process-data-deep-dive-50.js requires process-data-diagnostics.js');
if(!E||!E.sources)throw new Error('process-data-deep-dive-50.js requires MM_EVIDENCE_SOURCES');
if(PACKS.length!==5)throw new Error('process-data deep-dive requires five data packs');
const EXTRA_SOURCES={
 'sensor-review-2019':{name:'In-Mold Sensors for Injection Molding: On the Way to Industry 4.0',authority:'peer-reviewed research',kind:'review',url:'https://doi.org/10.3390/s19163551'},
 'measurement-review-2024':{name:'Zhao et al. (2024) — measurement techniques in injection molding',authority:'peer-reviewed research',kind:'review',url:'https://doi.org/10.1016/j.measurement.2024.114163'},
 'smart-sensor-review-2025':{name:'Shin et al. (2025) — in-situ and in-line monitoring with intelligent sensors',authority:'peer-reviewed research',kind:'review',url:'https://doi.org/10.1016/j.sna.2025.116248'},
 'quality-ml-2024':{name:'Ke (2024) — data-driven quality prediction in injection molding',authority:'peer-reviewed research',kind:'research',url:'https://doi.org/10.1002/pen.26866'},
 'predictive-maintenance-2026':{name:'Rebelo et al. (2026) — condition maintenance and prediction system in an injection molding machine',authority:'peer-reviewed research',kind:'case study',url:'https://doi.org/10.1108/JQME-05-2025-0050'},
 'ultrasound-review-2021':{name:'Kariminejad et al. (2021) — Ultrasound Sensors for Process Monitoring in Injection Moulding',authority:'peer-reviewed research',kind:'review',url:'https://doi.org/10.3390/s21155193'}
};
Object.assign(E.sources,EXTRA_SOURCES);
const RAW=PACKS.flatMap(p=>p.cases||[]);
if(RAW.length!==50)throw new Error(`process-data deep-dive expected 50 cases, got ${RAW.length}`);
const DEFS=RAW.map(r=>({id:r[0],title:r[1],kind:r[2],sourceIds:r[3],signals:Object.fromEntries(r[4].map(s=>[s[0],s.slice(1)])),fault:r[5],diagnosis:r[6],next:r[7]}));
if(new Set(DEFS.map(d=>d.id)).size!==50)throw new Error('process-data deep-dive case IDs must be unique');
function rng(seed){let x=(seed>>>0)||1;return()=>{x=(Math.imul(x,1664525)+1013904223)>>>0;return x/4294967296}}
function noise(r,scale){return(r()-0.5)*2*scale}
function generate(def,index){const r=rng(70000+index*211),rows=[];for(const [phase,p] of [['baseline',0],['fault',1],['recovery',2]])for(let cycle=1;cycle<=24;cycle++){const row={phase,cycle};for(const [key,v] of Object.entries(def.signals)){const base=+v[0],delta=+v[1],recovery=+v[2],target=p===0?base:p===1?base+delta:recovery,spread=Math.max(Math.abs(delta)*0.045,Math.abs(base)*0.0015,0.001);row[key]=+(target+noise(r,spread)).toFixed(4)}rows.push(row)}return{...def,synthetic:true,rows,phaseCounts:{baseline:24,fault:24,recovery:24},educationBoundary:'Synthetic training data only; relationships are illustrative and are not universal production setpoints, limits, maintenance thresholds or instructions.'}}
const DATASETS=DEFS.map(generate);
function mean(rows,key){const a=rows.map(r=>Number(r[key])).filter(Number.isFinite);return a.length?a.reduce((x,y)=>x+y,0)/a.length:0}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function label(k){return k.replace(/_/g,' ').replace(/([a-z])([A-Z])/g,'$1 $2')}
function sourceName(id){return E.sources[id]?.name||id}
function summary(ds){return Object.keys(ds.signals).map(key=>{const b=mean(ds.rows.filter(r=>r.phase==='baseline'),key),f=mean(ds.rows.filter(r=>r.phase==='fault'),key),r=mean(ds.rows.filter(r=>r.phase==='recovery'),key);return{key,b,f,r,d:f-b}})}
function pattern(ds){const rows=summary(ds).sort((a,b)=>Math.abs(b.d)-Math.abs(a.d)).slice(0,3);return rows.map(x=>`${label(x.key)} ${x.d>=0?'rises':'falls'} (${x.d>=0?'+':''}${x.d.toFixed(3)})`).join('; ')}
function csv(ds){const keys=['phase','cycle',...Object.keys(ds.signals)],lines=[keys.join(',')];for(const row of ds.rows)lines.push(keys.map(k=>row[k]).join(','));return lines.join('\n')+'\n'}
function host(){return document.getElementById('processDataLabs')}
function style(){if(document.getElementById('mm-dd50-style'))return;const s=document.createElement('style');s.id='mm-dd50-style';s.textContent=`.dd50-launch{margin-top:12px}.dd50-hero{padding:22px}.dd50-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.dd50-card{padding:16px;display:flex;flex-direction:column;gap:8px}.dd50-card p{margin:0;color:var(--muted);font-size:13px;line-height:1.5}.dd50-meta{display:flex;gap:6px;flex-wrap:wrap}.dd50-chip{font-size:10px;border:1px solid #3b5574;border-radius:999px;padding:4px 7px;color:#bcd1e8;background:#102137}.dd50-toolbar{display:flex;gap:8px;align-items:center;justify-content:space-between;flex-wrap:wrap;margin:14px 0}.dd50-tablewrap{overflow:auto;border:1px solid #2d4563;border-radius:10px}.dd50-table{width:100%;border-collapse:collapse;min-width:620px}.dd50-table th,.dd50-table td{padding:9px 10px;border-bottom:1px solid #253b55;text-align:right;font-size:12px}.dd50-table th:first-child,.dd50-table td:first-child{text-align:left}.dd50-note{padding:12px;border:1px solid #66582c;background:#282313;border-radius:10px;color:#f3e5ae;font-size:12px;line-height:1.5}@media(max-width:760px){.dd50-grid{grid-template-columns:1fr}.dd50-toolbar{align-items:stretch}.dd50-toolbar select,.dd50-toolbar button{width:100%}}`;document.head.appendChild(s)}
function attachLauncher(){const h=host();if(!h||h.querySelector('[data-dd50-root]')||h.querySelector('[data-dd50-launch]')||!h.querySelector('.pd-hero'))return;style();const b=document.createElement('button');b.type='button';b.className='secondary dd50-launch';b.dataset.dd50Launch='1';b.textContent='Open 50-case data deep dive';h.querySelector('.pd-hero').appendChild(b)}
function open(){BASE.open();requestAnimationFrame(()=>renderHome('all'))}
function renderHome(kind='all'){style();const h=host();if(!h)return;const list=kind==='all'?DATASETS:DATASETS.filter(d=>d.kind===kind),kinds=[...new Set(DATASETS.map(d=>d.kind))];h.innerHTML=`<div data-dd50-root class="dd50-hero card"><div class="eyebrow">Advanced process-data library</div><h2>50-case data deep dive</h2><p>Explore fifty additional deterministic datasets covering machine condition, tooling, materials, scientific moulding and measurement/quality sensing. Each case has 72 cycles: 24 baseline, 24 fault and 24 recovery.</p><div class="dd50-note"><b>Education boundary:</b> these datasets teach signal relationships and evidence selection. Values are synthetic and must not be copied as production settings, acceptance limits or maintenance thresholds.</div></div><div class="dd50-toolbar"><select data-dd50-kind aria-label="Filter deep-dive cases"><option value="all">All 50 cases</option>${kinds.map(k=>`<option value="${esc(k)}" ${k===kind?'selected':''}>${esc(k)} · ${DATASETS.filter(d=>d.kind===k).length}</option>`).join('')}</select><button class="ghost" data-dd50-guided>Back to guided 14 cases</button></div><div class="dd50-grid">${list.map(d=>`<article class="dd50-card card"><div class="dd50-meta"><span class="dd50-chip">${esc(d.kind)}</span><span class="dd50-chip">72 cycles</span><span class="dd50-chip">4 signals</span></div><h3>${esc(d.title)}</h3><p>${esc(d.fault)}</p><button class="secondary" data-dd50-open="${esc(d.id)}">Inspect dataset</button></article>`).join('')}</div>`}
function renderCase(id){const ds=DATASETS.find(d=>d.id===id);if(!ds)return renderHome();const h=host(),rows=summary(ds);h.innerHTML=`<div data-dd50-root><div class="dd50-toolbar"><button class="ghost" data-dd50-home>← 50-case library</button><button class="ghost" data-dd50-guided>Guided 14 cases</button></div><div class="card dd50-hero"><div class="dd50-meta"><span class="dd50-chip">${esc(ds.kind)}</span><span class="dd50-chip">synthetic training data</span></div><h2>${esc(ds.title)}</h2><p>${esc(ds.fault)}</p><div class="dd50-note">${esc(ds.educationBoundary)}</div></div><div class="card" style="padding:18px;margin-top:12px"><h3>Baseline → fault → recovery</h3><div class="dd50-tablewrap"><table class="dd50-table"><thead><tr><th>Signal</th><th>Baseline mean</th><th>Fault mean</th><th>Recovery mean</th><th>Fault Δ</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(label(r.key))}</td><td>${r.b.toFixed(3)}</td><td>${r.f.toFixed(3)}</td><td>${r.r.toFixed(3)}</td><td>${r.d>=0?'+':''}${r.d.toFixed(3)}</td></tr>`).join('')}</tbody></table></div><p><b>Read the pattern:</b> ${esc(pattern(ds))}</p><p><b>Ranked mechanism:</b> ${esc(ds.diagnosis)}</p><p><b>Best next evidence:</b> ${esc(ds.next)}</p><p class="tiny muted"><b>Evidence:</b> ${ds.sourceIds.map(sourceName).map(esc).join(' · ')}</p><button class="secondary" data-dd50-csv="${esc(ds.id)}">Export 72-cycle CSV</button></div></div>`}
function download(id){const ds=DATASETS.find(d=>d.id===id);if(!ds)return;const blob=new Blob([csv(ds)],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`mouldmaster-deep-dive-${ds.id}.csv`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
function click(e){const t=e.target.closest('[data-dd50-launch],[data-dd50-open],[data-dd50-home],[data-dd50-guided],[data-dd50-csv]');if(!t)return;if(t.hasAttribute('data-dd50-launch'))return open();if(t.dataset.dd50Open)return renderCase(t.dataset.dd50Open);if(t.hasAttribute('data-dd50-home'))return renderHome();if(t.hasAttribute('data-dd50-guided'))return BASE.open();if(t.dataset.dd50Csv)return download(t.dataset.dd50Csv)}
function change(e){if(e.target.matches('[data-dd50-kind]'))renderHome(e.target.value)}
document.addEventListener('click',click);document.addEventListener('change',change);
const originalOpen=BASE.open.bind(BASE);BASE.open=function(){const r=originalOpen();requestAnimationFrame(attachLauncher);return r};
attachLauncher();
window.MM_PROCESS_DATA_DEEP_DIVE_50={version:VERSION,reviewed:REVIEWED,reviewBy:REVIEW_BY,cases:DATASETS.map(d=>({id:d.id,title:d.title,kind:d.kind,signals:Object.keys(d.signals),sourceIds:d.sourceIds})),datasets:DATASETS,byId:id=>DATASETS.find(d=>d.id===id)||null,toCsv:id=>{const d=DATASETS.find(x=>x.id===id);return d?csv(d):''},open,scope:'50 additional deterministic synthetic process-data cases; educational evidence practice only, outside formal assessment and not a production recipe.'};
})();
