/* MouldMaster contextual research evidence UI — 2026.09.02.2 */
(function(){
'use strict';
const VERSION='2026.09.02.2';
const ENGINE=()=>window.MM_RESEARCH_EVIDENCE;
const ANALYTICS=()=>window.MM_RESEARCH_UTILISATION;
const GAPS=()=>window.MM_RESEARCH_GAPS;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function pct(v){return Number.isFinite(v)?`${Math.round(v*100)}%`:'Unknown'}
function ensureStyle(){if(document.getElementById('mm-research-evidence-ui-style'))return;const s=document.createElement('style');s.id='mm-research-evidence-ui-style';s.textContent=`
.mm-research-context{margin-top:14px;border:1px solid #385473;border-radius:12px;background:#0e1e31;padding:14px}.mm-research-context h3{margin:4px 0 8px}.mm-research-context>p{color:#b7c9dc;font-size:12px;line-height:1.55}.mm-research-grid{display:grid;gap:9px}.mm-research-card{border:1px solid #304a67;border-radius:10px;padding:12px;background:#102238}.mm-research-head{display:flex;justify-content:space-between;gap:8px;align-items:flex-start}.mm-research-head b{display:block}.mm-research-head small{display:block;color:#8198b2;font-size:9px;margin-top:3px}.mm-research-chips{display:flex;gap:5px;flex-wrap:wrap;margin:7px 0}.mm-research-chip{font-size:9px;border:1px solid #45627f;border-radius:999px;padding:3px 7px;color:#d4e2f0}.mm-research-card p{font-size:11px;color:#b6c7d9;line-height:1.45;margin:7px 0}.mm-research-cols{display:grid;grid-template-columns:1fr 1fr;gap:8px}.mm-research-mini{padding:9px;border-radius:8px;background:#0c1b2d;border:1px solid #263e59}.mm-research-mini strong{display:block;font-size:10px;margin-bottom:4px}.mm-research-mini ul{margin:0;padding-left:16px;color:#b8c9db;font-size:10px;line-height:1.45}.mm-research-plan{margin-top:9px}.mm-research-plan summary{cursor:pointer;font-size:11px;font-weight:800}.mm-research-plan-body{margin-top:8px;padding:10px;border-left:3px solid #537aa4;background:#0b1a2c}.mm-research-plan-body p{margin:5px 0}.mm-research-empty{font-size:11px;color:#91a8c1}.mm-research-quality{border-color:#4d8b78}.mm-research-app-high{border-color:#4d8b78}.mm-research-app-moderate{border-color:#8a7648}.mm-research-app-low{border-color:#89545c}.mm-research-scope{border-left:3px solid #d2b35c;padding-left:9px!important;color:#dacd9f!important}@media(max-width:720px){.mm-research-cols{grid-template-columns:1fr}}
`;document.head.appendChild(s)}
function hostContext(host){if(!host)return{text:''};const clone=host.cloneNode(true);clone.querySelectorAll('[data-mm-research-context],[data-mm-measured-evidence]').forEach(x=>x.remove());const fields=[...clone.querySelectorAll('input,textarea,select')].map(x=>x.value||'').join(' ');return {text:`${clone.textContent||''} ${fields}`}}
function normaliseContext(input){if(typeof input==='string')return{text:input};return input&&typeof input==='object'?input:{text:''}}
function list(items){return `<ul>${(items||[]).slice(0,3).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`}
function planHtml(r,context){const p=ENGINE()?.verificationPlan(context,r.id);if(!p)return'';return `<details class="mm-research-plan" data-mm-research-plan="${esc(r.id)}"><summary>Build verification plan</summary><div class="mm-research-plan-body"><p><b>Hypothesis:</b> ${esc(p.hypothesis)}</p><p><b>Strongest next check:</b> ${esc(p.strongestNextCheck)}</p><p><b>Recovery criterion:</b> ${esc(p.recoveryCriterion)}</p><p><b>Boundary:</b> ${esc(p.boundary)}</p><p><b>Safety:</b> ${esc(p.safety)}</p></div></details>`}
function card(r,context){const app=r.applicability||{};return `<article class="mm-research-card" data-mm-research-mechanism="${esc(r.id)}"><div class="mm-research-head"><div><b>${esc(r.title)}</b><small>${esc(r.id)}</small></div><span class="mm-research-chip mm-research-quality">Promoted evidence</span></div><div class="mm-research-chips"><span class="mm-research-chip">Evidence quality: High</span><span class="mm-research-chip mm-research-app-${esc(app.label||'unknown')}">Applicability: ${esc(app.label||'unknown')} ${app.score==null?'':`(${pct(app.score)})`}</span></div><p>${esc(r.claim)}</p><div class="mm-research-cols"><div class="mm-research-mini"><strong>Supports this mechanism</strong>${list(r.supports)}</div><div class="mm-research-mini"><strong>Would weaken it</strong>${list(r.weakens)}</div><div class="mm-research-mini"><strong>Alternative explanations</strong>${list(r.alternatives)}</div><div class="mm-research-mini"><strong>Strongest next evidence</strong><p>${esc(r.nextEvidence)}</p></div></div>${planHtml(r,context)}<p><b>Research boundary:</b> ${esc(r.limitation)}</p></article>`}
function recordRows(rows,surface){for(const r of rows)ANALYTICS()?.record?.('evidence_shown',{mechanismId:r.id,applicability:r.applicability?.label||'unknown',surface})}
function wireTelemetry(section,surface){section.querySelectorAll('[data-mm-research-plan]').forEach(d=>d.addEventListener('toggle',()=>{if(!d.open)return;const id=d.dataset.mmResearchPlan,card=d.closest('[data-mm-research-mechanism]'),app=card?.querySelector('[class*="mm-research-app-"]')?.className.match(/mm-research-app-(high|moderate|low|unknown)/)?.[1]||'unknown';ANALYTICS()?.record?.('verification_opened',{mechanismId:id,applicability:app,surface})},{once:true}))}
function render(host,input,limit=3,surface='context'){const e=ENGINE();if(!e||!host||host.querySelector(':scope > [data-mm-research-context]'))return null;const context=normaliseContext(input),rows=e.retrieve(context,limit);const html=`<section class="mm-research-context" data-mm-research-context="1" data-mm-research-surface="${esc(surface)}"><span class="eyebrow">Contextual research evidence</span><h3>What the research says — and what would disprove it</h3><p>Evidence strength and applicability are separate. These mechanisms can guide the next measurement, but they do not override local measured evidence or create universal settings.</p>${rows.length?`<div class="mm-research-grid">${rows.map(r=>card(r,context)).join('')}</div>`:'<div class="mm-research-empty">No promoted mechanism matched this context strongly enough to surface automatically.</div>'}<p class="mm-research-scope">Use local process actuals, physical quality, approved limits and recovery evidence to confirm the diagnosis.</p></section>`;host.insertAdjacentHTML('beforeend',html);const section=host.querySelector(':scope > [data-mm-research-context]');if(rows.length)recordRows(rows,surface);else{ANALYTICS()?.record?.('no_match',{surface,applicability:'unknown'});GAPS()?.add?.({reason:'no-match'})}if(section)wireTelemetry(section,surface);return {rows,section}}
function enrichMeasuredPanels(){document.querySelectorAll('[data-mm-measured-evidence="relevant"]').forEach(panel=>{const host=panel.parentElement;if(!host)return;render(host,hostContext(host),3,'measured-evidence')})}
function enrichProcessLabs(){const host=document.getElementById('processDataLabs');if(!host)return;const casePanel=host.querySelector('.pd-case,.pd-summary,.pd-panel');if(casePanel)render(host,hostContext(host),3,'process-labs')}
function enrichLesson(){const article=document.querySelector('#lesson article.lesson-body');if(article)render(article,hostContext(article),2,'lesson')}
let queued=false;function run(){queued=false;ensureStyle();enrichMeasuredPanels();enrichProcessLabs();enrichLesson()}
function schedule(){if(queued)return;queued=true;(window.requestAnimationFrame||setTimeout)(run,0)}
new MutationObserver(schedule).observe(document.documentElement,{subtree:true,childList:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule);else schedule();
window.MM_RESEARCH_EVIDENCE_UI={version:VERSION,render:(host,input,limit=3,surface='context')=>render(host,input,limit,surface),hostContext};
})();
