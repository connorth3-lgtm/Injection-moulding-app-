/* MouldMaster assessment runtime v2 — blueprint-preserving bank rotation 2026-09-02 */
(function(){
'use strict';
if(window.MM_ASSESSMENT_RUNTIME_V2)return;
const VERSION='2026.09.02.1';
const STORAGE_BASE='mm_assessment_membership_history_v2';
const MIN_BANK_PER_LEVEL=30;
const BLUEPRINT=['materials','machine','tooling','process','quality','troubleshooting'];
const D=window.MM_DATA,R=window.MM_RUNTIME_V2;
if(!D||!D.exams||!D.regionalQuestions)throw new Error('assessment-runtime-v2.js requires the canonical assessment bank');
if(!R||typeof R.setImplementation!=='function')throw new Error('assessment-runtime-v2.js requires runtime-v2.js');
if(typeof window.getExamQuestions!=='function')throw new Error('assessment-runtime-v2.js requires the audited assessment selector');
function storageKey(){return R.storage.key(STORAGE_BASE)}
function emptyHistory(){return {schema:2,version:VERSION,forms:{},items:{}}}
function readHistory(){const x=R.storage.get(STORAGE_BASE,null);return x&&x.schema===2&&x.version===VERSION&&x.items&&x.forms?x:emptyHistory()}
function writeHistory(x){x.version=VERSION;return R.storage.set(STORAGE_BASE,x)}
function resetHistory(){return R.storage.remove(STORAGE_BASE)}
function norm(v){return String(v??'').trim().toLowerCase().replace(/\s+/g,' ')}
function competencies(text){const t=norm(text),out=[];if(/resin|polymer|material|moisture|dry|mfr|mvr|rheolog|viscos|melt temp|crystalli|regrind|recycl|degrad/.test(t))out.push('materials');if(/machine|screw|cushion|recovery|non-return|check ring|barrel|controller|setpoint|injection unit|clamp|transfer position|hydraulic|servo/.test(t))out.push('machine');if(/mould|mold|cavity|gate|runner|vent|cooling|water line|parting line|ejection|hot runner|valve gate|surface temperature|tool/.test(t))out.push('tooling');if(/fill|pack|hold|gate seal|velocity|pressure|cycle|process window|transfer|shot|flow|shear|residence/.test(t))out.push('process');if(/cpk|ppk|capability|measurement|gauge|gage|doe|experiment|random|block|validation|specification|sample|control chart|quality|dimension|confidence|effect size|missing/.test(t))out.push('quality');if(/diagnos|troubleshoot|first|strongest|investigat|drift|changes|becomes|fails|defect|short shot|flash|sink|splay|burn|weld|warpage|brittle|disagree|evidence|alternative|recovery/.test(t))out.push('troubleshooting');return [...new Set(out)]}
function concept(text){const t=norm(text),defs=[['moisture-drying',/moisture|hygroscopic|dry/],['mfr-rheology',/\bmfr\b|\bmvr\b|rheolog|viscos/],['gate-seal',/gate seal|gate freeze|mass plateau/],['cavity-pressure',/cavity pressure|in-cavity|machine peak pressure/],['shot-delivery',/cushion|non-return|check ring|shot delivery|recovery/],['cooling-thermal',/cooling|water line|mould-surface|mold-surface|warpage/],['capability',/cpk|ppk|capability/],['measurement',/measurement|gauge|gage|fixture|confidence|missing/],['doe',/\bdoe\b|experiment|randomis|randomiz|blocking|confound|interaction/],['process-transfer',/receiving machine|process equivalence|transfer strategy/],['setpoint-actual',/setpoint|saved recipe|known-good baseline/],['tooling-locality',/one cavity|local flow|branch|parting line|gate wear/]];for(const [id,re] of defs)if(re.test(t))return id;return t.split(/[^a-z0-9]+/).filter(x=>x.length>4).slice(0,4).join('-')||'general'}
function tech(level,i,q){const text=q?.q??q?.[0]??'',options=q?.options??q?.[1]??[],correct=Number(q?.correct??q?.[2]??0),feedback=q?.optionFeedback??q?.feedback??q?.[6]??[];const cs=competencies(text);return {q:text,options:[...options],correct,explanation:q?.explanation??q?.why??q?.[3]??'',reference:q?.reference??q?.source??q?.[4]??'',sourceUrl:q?.sourceUrl??q?.url??q?.[5]??null,optionFeedback:[...feedback],critical:!!(q?.critical??q?.[7]),kind:'technical',level,bankIndex:i,stableId:`tech:${level}:${i}`,mmId:`tech:${level}:${i}`,competencies:cs,competency:cs[0]||BLUEPRINT[i%BLUEPRINT.length],concept:concept(text)}}
function regional(region,level,i,q){return {q:q?.q??q?.[0]??'',options:[...(q?.options??q?.[1]??[])],correct:Number(q?.correct??q?.[2]??0),explanation:q?.explanation??q?.why??q?.[3]??'',reference:q?.reference??q?.source??q?.[4]??'',sourceUrl:q?.sourceUrl??q?.url??q?.[5]??null,optionFeedback:[...(q?.optionFeedback??q?.feedback??q?.[6]??[])],critical:(q?.critical??q?.[7])!==false,kind:'regional',region,level,bankIndex:i,stableId:`reg:${region}:${level}:${i}`,mmId:`reg:${region}:${level}:${i}`,competencies:['safety'],competency:'safety',concept:`safety-${region.toLowerCase()}-${i}`}}
function seeded(seed){let x=2166136261;for(const c of String(seed)){x^=c.charCodeAt(0);x=Math.imul(x,16777619)}return ()=>{x=(Math.imul(x,1664525)+1013904223)>>>0;return x/4294967296}}
function shuffle(a,rng){const x=a.slice();for(let i=x.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[x[i],x[j]]=[x[j],x[i]]}return x}
function shuffleOptions(item,rng){const rows=item.options.map((text,i)=>({text,correct:i===item.correct,feedback:item.optionFeedback?.[i]??null}));const mixed=shuffle(rows,rng);return {...item,options:mixed.map(x=>x.text),optionFeedback:mixed.map(x=>x.feedback),correct:mixed.findIndex(x=>x.correct)}}
function exposure(history,id){return history.items[id]||{count:0,last:-1}}
function rank(pool,history,rng){const tie=new Map(pool.map(x=>[x.stableId,rng()]));return pool.slice().sort((a,b)=>{const ea=exposure(history,a.stableId),eb=exposure(history,b.stableId);return ea.count-eb.count||ea.last-eb.last||tie.get(a.stableId)-tie.get(b.stableId)})}
function selectTechnical(level,history,formNumber,rng){const pool=(D.exams[level]||[]).map((q,i)=>tech(level,i,q));if(pool.length<MIN_BANK_PER_LEVEL)throw new Error(`assessment runtime v2 requires at least ${MIN_BANK_PER_LEVEL} technical items for ${level}; found ${pool.length}`);const chosen=[],used=new Set(),add=item=>{if(!item||used.has(item.stableId))return false;chosen.push(item);used.add(item.stableId);return true};for(const domain of BLUEPRINT){const candidates=pool.filter(x=>!used.has(x.stableId)&&x.competencies.includes(domain));if(candidates.length)add(rank(candidates,history,rng)[0])}while(chosen.length<7){const remaining=pool.filter(x=>!used.has(x.stableId));if(!remaining.length)break;const concepts=new Set(chosen.map(x=>x.concept)),diverse=remaining.filter(x=>!concepts.has(x.concept));add(rank(diverse.length?diverse:remaining,history,rng)[0])}if(chosen.length!==7)throw new Error(`assessment runtime v2 could select only ${chosen.length}/7 technical items for ${level}`);const covered=new Set();for(const q of chosen)for(const c of q.competencies)covered.add(c);const missing=BLUEPRINT.filter(x=>!covered.has(x));if(missing.length)throw new Error(`assessment runtime v2 blueprint incomplete for ${level}: ${missing.join(', ')}`);return chosen}
function selectRegional(region,level,rng){if(region==='ALL'){const out=[];for(const r of ['UK','US','NZ'])for(let i=0;i<(D.regionalQuestions[r]?.[level]||[]).length;i++)out.push(regional(r,level,i,D.regionalQuestions[r][level][i]));return out}return shuffle((D.regionalQuestions[region]?.[level]||[]).map((q,i)=>regional(region,level,i,q)),rng).slice(0,3)}
function nextExam(level,region){const history=readHistory(),formNumber=Number(history.forms[level]||0)+1,rng=seeded(`${R.storage.learnerToken()}:${level}:${region}:${formNumber}`);const technical=selectTechnical(level,history,formNumber,rng),regs=selectRegional(region,level,rng),selected=[...technical,...regs];history.forms[level]=formNumber;for(const q of technical){const e=exposure(history,q.stableId);history.items[q.stableId]={count:e.count+1,last:formNumber}}writeHistory(history);return shuffle(selected,rng).map(q=>shuffleOptions(q,rng))}
const legacySelector=window.getExamQuestions;
function selector(level,region){if(!['Beginner','Intermediate','Advanced'].includes(level)||!['UK','US','NZ','ALL'].includes(region))return legacySelector.apply(this,arguments);return nextExam(level,region)}
R.setImplementation('getExamQuestions',selector,'assessment-runtime-v2');R.registerModule('assessment-runtime-v2',{version:VERSION,type:'assessment-selector',owns:'getExamQuestions'});
function coverageSimulation(level,attempts=6){const history=emptyHistory(),seen=new Set();for(let n=1;n<=attempts;n++){const rng=seeded(`qa:${level}:${n}`),rows=selectTechnical(level,history,n,rng);for(const q of rows){seen.add(q.stableId);const e=exposure(history,q.stableId);history.items[q.stableId]={count:e.count+1,last:n}}}return {level,attempts,seen:[...seen],coverage:seen.size,total:(D.exams[level]||[]).length}}
window.MM_ASSESSMENT_RUNTIME_V2=Object.freeze({version:VERSION,blueprint:[...BLUEPRINT],technicalPerExam:7,minimumTechnicalBankPerLevel:MIN_BANK_PER_LEVEL,technicalBankPerLevel:()=>Object.fromEntries(['Beginner','Intermediate','Advanced'].map(level=>[level,(D.exams[level]||[]).length])),membershipHistory:'Learner-scoped persistent exposure counts; displaying an item counts as exposure.',selectionPolicy:'Least-exposed, blueprint-preserving stable IDs; all six domains required every generated form. Expanded banks must contain independently authored items and may not be created by runtime duplication.',storageKey,resetHistory,history:()=>JSON.parse(JSON.stringify(readHistory())),coverageSimulation});
})();
