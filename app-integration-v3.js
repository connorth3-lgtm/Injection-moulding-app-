/* MouldMaster app integration v3 — canonical cross-app routing, evidence state and continuity — 2026.09.02 */
(function(){
'use strict';
if(window.MM_APP_INTEGRATION)return;
const VERSION='2026.09.02.1';
const LEARNING_PREFIX='mm_learning_analytics_v1::';
const COHORT_KEY='mm_cohort_calibration_v1';
const COMPETENCY_ORDER=['viewed','completed','practised','demonstrated','transferred','retained','assessed'];
const ALIASES=new Map([['diagnose','mould-master'],['problem-now','mould-master'],['due-reviews','learning-insights']]);
const bus=new Map();
const state={route:'',focus:null,dueReview:null};
const safe=(fn,...args)=>{try{return typeof fn==='function'?fn(...args):undefined}catch(e){console.warn('[MouldMaster integration]',e);return undefined}};
const clone=v=>{try{return structuredClone(v)}catch(_){try{return JSON.parse(JSON.stringify(v))}catch(_){return v}}};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function emit(name,payload){for(const fn of bus.get(name)||[])safe(fn,clone(payload))}
function on(name,fn){if(typeof fn!=='function')return()=>{};if(!bus.has(name))bus.set(name,new Set());bus.get(name).add(fn);return()=>bus.get(name)?.delete(fn)}
function off(name,fn){bus.get(name)?.delete(fn)}
function getState(){return clone(state)}
function setState(next){for(const k of Object.keys(state))delete state[k];Object.assign(state,clone(next)||{});emit('state',getState());return getState()}
function patchState(patch){Object.assign(state,clone(patch)||{});emit('state',getState());return getState()}
function subscribeState(fn){const stop=on('state',fn);safe(fn,getState());return stop}
function registerAlias(from,to){if(from&&to)ALIASES.set(String(from),String(to));return()=>ALIASES.delete(String(from))}
function resolveRoute(route){let r=String(route||'');const seen=new Set();while(ALIASES.has(r)&&!seen.has(r)){seen.add(r);r=ALIASES.get(r)}return r}
function upgradeShell(){
 const shell=window.MM_APP_SHELL;if(!shell)return false;
 Object.assign(shell,{on,off,emit,getState,setState,patchState,subscribeState,registerAlias,resolveRoute});
 if(!shell.__mmIntegratedGo&&typeof shell.go==='function'){
  const base=shell.go.bind(shell);shell.go=function(route,options){const resolved=resolveRoute(route);patchState({route:resolved});emit('route',{requested:String(route||''),resolved,options:clone(options||{})});return base(resolved,options)};shell.__mmIntegratedGo=true;
 }
 return true
}
function openWorkspace(){
 patchState({route:'mould-master',focus:'problem-now'});emit('route',{requested:'diagnose',resolved:'mould-master'});
 if(window.MM_MOULD_MASTER_WORKSPACE?.open)return safe(window.MM_MOULD_MASTER_WORKSPACE.open);
 if(window.MM_APP_SHELL?.go)return safe(window.MM_APP_SHELL.go,'mould-master');
 const root=document.getElementById('mmMouldMasterWorkspace');if(root){root.scrollIntoView({behavior:'smooth',block:'start'});root.focus?.();return true}return false
}
function bindProblemEntry(){document.addEventListener('click',e=>{const b=e.target.closest?.('button,a');if(!b)return;const text=(b.textContent||'').trim();if(/diagnose a moulding problem|i have a problem now/i.test(text)){e.preventDefault();e.stopImmediatePropagation();openWorkspace()}},true)}
function activeUserId(){try{if(typeof db!=='undefined'&&db?.activeUser)return String(db.activeUser)}catch(_){}try{if(typeof user!=='undefined'&&user?.id)return String(user.id)}catch(_){}return'anonymous'}
function tokenFor(raw){let h=2166136261;for(const ch of String(raw||'anonymous')){h^=ch.charCodeAt(0);h=Math.imul(h,16777619)}return(h>>>0).toString(36)}
function learnerEvents(){try{const x=JSON.parse(localStorage.getItem(LEARNING_PREFIX+tokenFor(activeUserId()))||'null');return Array.isArray(x?.events)?x.events:[]}catch(_){return[]}}
function completedSet(){try{return new Set((user?.completed||[]).map(String))}catch(_){return new Set()}}
function lessonIdNow(){try{return String(typeof currentLesson==='function'?currentLesson()?.id||'':'')}catch(_){return''}}
function viewedKey(){return`mm_competency_viewed_v1::${tokenFor(activeUserId())}`}
function markViewed(){const id=lessonIdNow();if(!id)return;try{const x=JSON.parse(localStorage.getItem(viewedKey())||'[]'),s=new Set(Array.isArray(x)?x.map(String):[]);if(!s.has(id)){s.add(id);localStorage.setItem(viewedKey(),JSON.stringify([...s].slice(-500)))}}catch(_){}}
function viewedSet(){try{const x=JSON.parse(localStorage.getItem(viewedKey())||'[]');return new Set((Array.isArray(x)?x:[]).map(String))}catch(_){return new Set()}}
function assessedCount(){
 let n=0;try{const u=typeof user!=='undefined'?user:null;if(!u)return 0;const candidates=[u.examResults,u.assessments,u.examHistory,u.certificates,u.certs];for(const value of candidates){if(Array.isArray(value))n=Math.max(n,value.filter(x=>x&&(x.passed===true||x.pass===true||Number(x.score)>=80||x.earned===true)).length);else if(value&&typeof value==='object')n=Math.max(n,Object.values(value).filter(x=>x&&(x===true||x.passed===true||x.pass===true||Number(x.score)>=80||x.earned===true)).length)}}catch(_){}return n
}
function competencySummary(){
 const viewed=viewedSet(),completed=completedSet(),events=learnerEvents(),practice=new Set(),demonstrated=new Set(),transferred=new Set(),retained=new Set(),contexts=new Map();
 for(const x of events){const id=String(x.lessonId||x.lesson||x.id||'');if(x.type==='practice_complete'){practice.add(id);if(Number(x.score)>=100||x.correct===true)demonstrated.add(id);if(/^run-insight-/.test(id)&&(Number(x.score)>=100||x.correct===true)){const m=id.match(/^run-insight-(.+?)-(evidence|falsification|recovery|integration)(?:-([a-z0-9]+))?$/);if(m){const key=`${m[1]}:${m[2]}`,set=contexts.get(key)||new Set();set.add(m[3]||'legacy');contexts.set(key,set);if(set.size>=2)transferred.add(key)}}}if(x.type==='retention_check'&&(Number(x.score)>=100||x.correct===true))retained.add(id)}
 return {viewed:viewed.size,completed:completed.size,practised:practice.size,demonstrated:demonstrated.size,transferred:transferred.size,retained:retained.size,assessed:assessedCount(),order:COMPETENCY_ORDER.slice(),boundary:'Completion records finishing a lesson. Demonstrated, transferred, retained and assessed are separate evidence states and are never inferred from lesson completion alone.'}
}
function dueReviews(){return window.MM_LEARNING_EFFECTIVENESS?.dueTransferChecks?.()||[]}
function mechanismContext(item){const id=String(item?.mechanismId||'');const title=window.MM_RESEARCH_EVIDENCE?.retrieve?.({text:id.replace(/-/g,' '),process:['injection moulding']},4)?.find(x=>x.id===id)?.title||id.replace(/-/g,' ');return{text:`Delayed transfer review for ${title}`,process:['injection moulding'],signals:['independent measured actual'],outcomes:['physical quality response']}}
function duePractice(item){
 const practice=window.MM_RESEARCH_MICROLEARNING?.buildPractice?.(mechanismContext(item),{stage:item.stage,mode:'retention',retentionIntervalDays:item.intervalDays,observed:'A fresh transfer case is presented without the original run history. Choose the evidence reasoning that remains valid in this new context.'});
 return practice?.mechanismId===item.mechanismId?practice:null
}
function openDueReview(item){if(!item)return;patchState({dueReview:clone(item),route:'learning-insights'});emit('due-review-opened',item);if(window.MM_APP_SHELL?.go)safe(window.MM_APP_SHELL.go,'learning-insights');else safe(window.MM_LEARNING_ANALYTICS_UI?.open);requestAnimationFrame(()=>renderDueReviewFocus(item))}
function renderDueReviewFocus(item){
 const root=document.getElementById('learningInsights')||document.getElementById('dashboard');if(!root)return;root.querySelector('[data-mm-due-review-focus]')?.remove();const p=duePractice(item),box=document.createElement('section');box.dataset.mmDueReviewFocus='1';box.className='mm-integrated-card';
 if(!p){box.innerHTML=`<span class="eyebrow">Due review</span><h3>${esc(item.intervalDays)}-day transfer check</h3><p>Re-test <b>${esc(item.stage)}</b> reasoning for <b>${esc(String(item.mechanismId||'').replace(/-/g,' '))}</b>. State what would support the explanation, what would weaken it, and what recovery would need to look like.</p><p><small>Formative only. It does not reveal or modify formal assessment items.</small></p>`;root.prepend(box);return}
 box.dataset.mmDuePractice=p.id;box.innerHTML=`<span class="eyebrow">Due review · ${esc(item.intervalDays)} day</span><h3>${esc(p.prompt)}</h3><div class="mm-due-options">${p.options.map((o,i)=>`<button type="button" class="secondary" data-mm-due-choice="${i}">${esc(o.text)}</button>`).join('')}</div><div class="mm-due-feedback" aria-live="polite"></div><p><small>${esc(p.boundary)}</small></p>`;root.prepend(box);
 box.querySelectorAll('[data-mm-due-choice]').forEach(button=>button.addEventListener('click',()=>{if(box.dataset.mmAnswered==='1')return;box.dataset.mmAnswered='1';const i=Number(button.dataset.mmDueChoice),choice=p.options[i],correct=!!choice?.correct;box.querySelectorAll('[data-mm-due-choice]').forEach((b,j)=>{b.disabled=true;if(p.options[j]?.correct)b.dataset.correct='1';else if(j===i)b.dataset.wrong='1'});const fb=box.querySelector('.mm-due-feedback');fb.innerHTML=`<b>${correct?'Retained reasoning demonstrated.':'Review still due.'}</b><p>${esc(choice?.feedback||p.rationale)}</p>${!correct&&choice?.misconception?`<p><small>Reasoning pattern to revisit: ${esc(choice.misconception.replace(/-/g,' '))}</small></p>`:''}`;window.MM_LEARNING_EFFECTIVENESS?.recordOutcome?.(p,correct,0);if(!correct&&choice?.misconception)window.MM_LEARNING_ANALYTICS?.record?.('practice_misconception',{module:'process-data',id:p.id,reason:choice.misconception,correct:false});emit('due-review-answered',{mechanismId:p.mechanismId,stage:p.stage,intervalDays:p.retentionIntervalDays,correct})}))
}
function ensureStyle(){if(document.getElementById('mm-app-integration-v3-style'))return;const s=document.createElement('style');s.id='mm-app-integration-v3-style';s.textContent=`.mm-integrated-card{margin:12px 0;padding:14px;border:1px solid #395875;border-radius:13px;background:#0d1b2a}.mm-integrated-card h3{margin:5px 0 8px}.mm-integrated-card p{margin:6px 0;color:#bfd0df;line-height:1.45}.mm-integrated-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.mm-integrated-cell{padding:9px;border:1px solid #2c4962;border-radius:9px;background:#091624}.mm-integrated-cell small{display:block;color:#8faabe}.mm-integrated-cell b{display:block;margin-top:3px}.mm-integrated-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:9px}.mm-due-options{display:grid;grid-template-columns:1fr 1fr;gap:8px}.mm-due-options [data-correct="1"]{outline:2px solid #59c892}.mm-due-options [data-wrong="1"]{outline:2px solid #d77b73}.mm-due-feedback{margin-top:10px;padding:10px;border-left:3px solid #5b91bd;background:#0a1723}@media(max-width:760px){.mm-integrated-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.mm-due-options{grid-template-columns:1fr}}`;document.head.appendChild(s)}
function dashboardSections(){
 const shell=window.MM_APP_SHELL;if(!shell?.registerDashboard||shell.__mmAppWideSections)return;shell.__mmAppWideSections=true;
 shell.registerDashboard({id:'app-wide-due-reviews',zone:'before',order:8,when:()=>dueReviews().length>0,render:slot=>{const due=dueReviews();slot.innerHTML=`<section class="mm-integrated-card"><span class="eyebrow">Due reviews</span><h3>${due.length} transfer check${due.length===1?'':'s'} due</h3><p>Retention reviews are available from Home; you do not need to wait for a comparable production run.</p><div class="mm-integrated-actions">${due.slice(0,4).map((x,i)=>`<button type="button" class="secondary" data-mm-open-due="${i}">${esc(x.intervalDays)}d · ${esc(x.stage)} · ${esc(String(x.mechanismId).replace(/-/g,' '))}</button>`).join('')}</div></section>`;slot.querySelectorAll('[data-mm-open-due]').forEach(b=>b.addEventListener('click',()=>openDueReview(due[Number(b.dataset.mmOpenDue)])))}});
 shell.registerDashboard({id:'app-wide-competency',zone:'after',order:92,render:slot=>{const c=competencySummary();slot.innerHTML=`<section class="mm-integrated-card"><span class="eyebrow">Competency evidence</span><h3>Progress is more than lesson completion</h3><div class="mm-integrated-grid">${COMPETENCY_ORDER.map(k=>`<div class="mm-integrated-cell"><small>${esc(k[0].toUpperCase()+k.slice(1))}</small><b>${Number(c[k]||0)}</b></div>`).join('')}</div><p><small>Viewed and completed are activity states. Demonstrated, transferred, retained and assessed require separate evidence.</small></p></section>`}})
}
async function processWorkspaceReport(){const api=window.MM_CONNECTED_PROCESS_DATA?.storage;if(!api?.listDatasets)return{schema:1,datasets:[]};const ds=await api.listDatasets();return{schema:1,generatedAt:new Date().toISOString(),scope:'device-site-workspace',rawRowsIncluded:false,datasets:ds.map(x=>({id:x.id,createdAt:x.createdAt,rowCount:x.rowCount,headers:x.headers,entities:x.entities,datasetMeta:x.datasetMeta,quality:x.quality,evidenceState:x.evidenceState,authority:x.authority}))}}
async function exportProcessWorkspace(){const report=await processWorkspaceReport(),blob=new Blob([JSON.stringify(report,null,2)],{type:'application/json;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='mouldmaster-process-workspace-metadata.json';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);return report}
async function clearProcessWorkspace(){const api=window.MM_CONNECTED_PROCESS_DATA?.storage;if(!api?.listDatasets||!api?.deleteDataset)return 0;const ds=await api.listDatasets();for(const x of ds)await api.deleteDataset(x.id);emit('process-workspace-cleared',{count:ds.length});return ds.length}
function decorateProcessPrivacy(){for(const root of [document.querySelector('[data-pi-root]'),document.querySelector('[data-di-root]')].filter(Boolean)){if(root.querySelector('[data-mm-workspace-boundary]'))continue;const box=document.createElement('section');box.dataset.mmWorkspaceBoundary='1';box.className='mm-integrated-card';box.innerHTML=`<b>Local site/process workspace</b><p>Raw source files are not uploaded. Prepared datasets can persist in this browser/device IndexedDB until you delete them. They are device/site workspace data, not learner-profile data; changing learner profile does not create a separate process workspace.</p><div class="mm-integrated-actions"><button type="button" class="ghost" data-mm-process-export>Export metadata only</button><button type="button" class="ghost" data-mm-process-clear>Delete saved datasets</button></div>`;root.appendChild(box)}}
function normalCdfApprox(z){return .5*(1+Math.sign(z)*Math.sqrt(1-Math.exp(-2*z*z/Math.PI)))}
function uncertaintyFromSignal(s){const b=Number(s.baselineMean),c=Number(s.currentMean),z=Number(s.normalizedShift),delta=Number.isFinite(b)&&Number.isFinite(c)?c-b:null,nb=Number(s.baselineN||s.nBaseline||0)||null,nc=Number(s.currentN||s.nCurrent||0)||null,sdB=Number(s.baselineSd||s.sdBaseline),sdC=Number(s.currentSd||s.sdCurrent);let ci95=null;if(nb&&nc&&Number.isFinite(sdB)&&Number.isFinite(sdC)){const se=Math.sqrt(sdB*sdB/nb+sdC*sdC/nc);if(Number.isFinite(se)&&delta!=null)ci95=[delta-1.96*se,delta+1.96*se]}
 return{sampleSizeBaseline:nb,sampleSizeCurrent:nc,missingRateBaseline:Number.isFinite(Number(s.baselineMissingRate))?Number(s.baselineMissingRate):null,missingRateCurrent:Number.isFinite(Number(s.currentMissingRate))?Number(s.currentMissingRate):null,delta,effectSizeApprox:Number.isFinite(z)?Math.abs(z):null,approxMeanDifference95CI:ci95,approxTwoSidedTail:Number.isFinite(z)?Math.max(0,Math.min(1,2*(1-normalCdfApprox(Math.abs(z))))):null,interpretation:!Number.isFinite(z)?'insufficient-context':Math.abs(z)>=3?'large-local-shift':Math.abs(z)>=2?'moderate-local-shift':'small-or-stable',boundary:'Descriptive site-local evidence only. Approximate standardized shifts and confidence intervals are not causal proof, specification limits, validated process windows or machine safety limits.'}}
function enhanceProcessStatistics(){
 const intel=window.MM_CONNECTED_PROCESS_DATA?.intelligence;if(!intel||intel.__mmEvidenceEnhanced)return false;
 if(typeof intel.compareToBaseline==='function'){const base=intel.compareToBaseline.bind(intel);intel.compareToBaseline=async function(){const out=await base(...arguments);if(Array.isArray(out?.signals))out.signals=out.signals.map(s=>({...s,uncertainty:uncertaintyFromSignal(s)}));out.evidenceBoundary='Interpret magnitude with sample count, missingness, temporal stability, cavity/site context and measurement quality. Association and research relevance do not establish causation.';return out}}
 if(typeof intel.compareWindows==='function'){const base=intel.compareWindows.bind(intel);intel.compareWindows=function(){const out=base(...arguments);if(Array.isArray(out?.changes))out.changes=out.changes.map(s=>({...s,uncertainty:{effectSizeApprox:Number.isFinite(Number(s.normalizedChange))?Math.abs(Number(s.normalizedChange)):null,boundary:'Window comparison is descriptive and local. Check sample balance, missingness, time ordering and measurement quality before interpreting the change.'}}));out.evidenceBoundary='Before/after windows preserve time ordering and remain descriptive; a coincident intervention is not by itself causal proof.';return out}}
 intel.__mmEvidenceEnhanced=true;return true
}
function validateCohort(payload){if(!payload||typeof payload!=='object'||!Array.isArray(payload.items))throw new Error('Invalid cohort calibration payload');const text=JSON.stringify(payload);if(/learner|email|\"name\"|answerText|notes|timestamp|rawRows|shot/i.test(text))throw new Error('Cohort import contains prohibited identifying or raw-data fields');const profiles=Number(payload.anonymousProfiles||payload.profileCount||0);if(profiles<5)throw new Error('At least 5 anonymous profiles are required');const items=payload.items.map(x=>{if(!x.mechanismId||!x.stage||Number(x.attempts)<1)throw new Error('Cohort item missing aggregate fields');return{mechanismId:String(x.mechanismId),stage:String(x.stage),attempts:Number(x.attempts),successRate:Number.isFinite(Number(x.successRate))?Number(x.successRate):null,discrimination:Number.isFinite(Number(x.discrimination))?Number(x.discrimination):null,difficultyQuality:String(x.difficultyQuality||x.quality||'unknown'),calibratedChallenge:String(x.calibratedChallenge||x.challenge||'standard')}});return{schema:1,anonymousProfiles:profiles,items,importedAt:new Date().toISOString(),privacy:'Aggregate calibration only; no learner identifiers, answers, notes, exact event timestamps or process rows.'}}
function importCohort(payload){const clean=validateCohort(payload);localStorage.setItem(COHORT_KEY,JSON.stringify(clean));emit('cohort-calibration-imported',{anonymousProfiles:clean.anonymousProfiles,items:clean.items.length});return clean}
function cohortCalibration(){try{return clone(JSON.parse(localStorage.getItem(COHORT_KEY)||'null'))}catch(_){return null}}
function exportCohort(){const report=window.MM_LEARNING_EFFECTIVENESS?.anonymousReport?.();if(!report)return null;const blob=new Blob([JSON.stringify(report,null,2)],{type:'application/json;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='mouldmaster-anonymous-cohort-calibration.json';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);return report}
function cohortChallenge(mechanismId,stage,fallback='standard'){const cohort=cohortCalibration();if(!cohort||cohort.anonymousProfiles<5)return fallback;const x=cohort.items?.find(v=>v.mechanismId===mechanismId&&v.stage===stage);if(!x||x.attempts<12)return fallback;return['support','standard','stretch'].includes(x.calibratedChallenge)?x.calibratedChallenge:fallback}
function assessmentAudit(){const exams=window.MM_DATA?.exams||{},levels={};let ok=true;for(const level of ['Beginner','Intermediate','Advanced']){const rows=Array.isArray(exams[level])?exams[level]:[],stems=rows.map(q=>String(q?.q??q?.[0]??'').toLowerCase().replace(/\W+/g,' ').trim()).filter(Boolean),unique=new Set(stems);levels[level]={items:rows.length,uniqueStems:unique.size,minimumTarget:30,ready:rows.length>=30&&unique.size===rows.length};if(!levels[level].ready)ok=false}return{ok,levels,boundary:'Formal-bank readiness requires at least 30 independently authored unique technical items per level. Runtime duplication or stem cloning is not accepted as bank expansion.'}}
function accessibilityAudit(root=document){const issues=[];root.querySelectorAll('button,[role="button"],a[href]').forEach(el=>{const name=(el.getAttribute('aria-label')||el.getAttribute('title')||el.textContent||'').trim();if(!name||/^action$/i.test(name))issues.push({type:'control-name',tag:el.tagName})});root.querySelectorAll('img').forEach(img=>{if(!img.hasAttribute('alt'))issues.push({type:'missing-alt',src:(img.getAttribute('src')||'').slice(0,80)});else if(img.alt===''&&!img.dataset.decorative&&!img.closest('[aria-hidden="true"]'))issues.push({type:'unmarked-empty-alt',src:(img.getAttribute('src')||'').slice(0,80)})});return{ok:issues.length===0,issues}}
function installActions(){document.addEventListener('click',e=>{const ex=e.target.closest?.('[data-mm-process-export]');if(ex){e.preventDefault();exportProcessWorkspace()}const cl=e.target.closest?.('[data-mm-process-clear]');if(cl){e.preventDefault();if(confirm('Delete all saved prepared process datasets from this device? Raw source files are not affected.'))clearProcessWorkspace().then(()=>decorateProcessPrivacy())}},true)}
function install(){ensureStyle();upgradeShell();bindProblemEntry();installActions();enhanceProcessStatistics();dashboardSections();markViewed();decorateProcessPrivacy();const observer=new MutationObserver(()=>{upgradeShell();dashboardSections();markViewed();decorateProcessPrivacy();enhanceProcessStatistics()});observer.observe(document.documentElement,{childList:true,subtree:true})}
window.MM_APP_INTEGRATION=Object.freeze({version:VERSION,on,off,emit,getState,setState,patchState,subscribeState,registerAlias,resolveRoute,openWorkspace,competencySummary,dueReviews,openDueReview,processWorkspaceReport,exportProcessWorkspace,clearProcessWorkspace,importCohort,exportCohort,cohortCalibration,cohortChallenge,assessmentAudit,accessibilityAudit,scope:'One cross-app integration layer for routing, state/events, competency evidence, due reviews, local process-workspace continuity, aggregate cohort calibration and evidence-strength annotations. It does not create machine control, universal setpoints, causal proof or formal answer keys.'});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
